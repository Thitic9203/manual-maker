# แผนงาน — สร้าง PRD ราย Epic ของระบบ OLS จาก Jira (confluence-docs)

> ขอบเขต: **เฉพาะ subsystem OLS (Open Learning System)** — subsystem อื่นเป็นงานคนละรอบ
>
> งานสร้างชุดเอกสาร PRD ราย Epic ในพื้นที่เอกสารของทีม โดยดึงเนื้อหาจาก Jira ทั้งหมด
> ตามหลัก **1 Story = 1 Use Case** และกฎ **ห้ามมโน · ทุกค่ามีที่มา · ตรวจไม่ได้ = ไม่ผ่าน**
>
> เอกสารฉบับนี้จงใจ **ไม่ระบุ ID ภายใน** (เลขหน้า, คีย์ตั๋ว, cloudId, ลิงก์ภายใน)
> เพราะ repo เป็นสาธารณะ — รายละเอียดเชิงพิกัดอยู่ในบันทึกงานนอก repo

_อัปเดตล่าสุด: 2026-07-22 (รอบ 4 — connector auth กลับมาแล้ว (write scope ยืนยันด้วย `getAccessibleAtlassianResources`) → **action 12 กำลังเขียนจริง**: อ่านครบ 17/17 หน้า ยืนยัน id ตรงทุกครั้ง (กัน bug connector คืนหน้าผิด) → เขียนสำเร็จ+อ่านกลับยืนยันแล้ว **6/17 หน้า**: หน้าแม่ (ลบ panel ล้าสมัย) + OLS-1, OLS-2, OLS-3, OLS-4, OLS-5 (รวม TBC ที่แปลงแล้ว 46/138 จุด) · เหลือ OLS-6, 7, 8, 9, 10, 11, 12, 13, 17 (92 จุด) — OLS-14/15 ข้าม (0 จุด, ยังไม่จัดทำ Use Case) · เซสชันหน้าทำต่อจาก OLS-6 ได้เลย)_

## ตาราง Action ที่เหลือ

| # | Action | สถานะ | ใครตัดสิน | หมายเหตุ |
|---|--------|-------|-----------|----------|
| 0 | **ปลดบล็อกสิทธิ์เขียน connector** ให้ tool เขียนโผล่ใน session | 🟢 เสร็จ | — | session ใหม่: `updateConfluencePage`/`createConfluencePage` เข้า manifest แล้ว + scope `write:page:confluence` ครบ (ยืนยันด้วย `getAccessibleAtlassianResources`) — เขียนได้จริง |
| 1 | แก้ **คำพราก** locked term ในหน้าย่อย 1 หน้า ให้ผ่าน verifier | 🟢 เสร็จ | อัตโนมัติ | OLS-13: locked term `คอร์สเรียน` เป็น false positive ที่ขอบวลี `ลงทะเบียนคอร์ส` + `เรียนจบคอร์ส` — แทรก `และ` 2 จุด (ไม่เปลี่ยนความหมาย) · verifier term-check ✅ · อ่านกลับ live: ใหม่ 2 / เก่า 0 |
| 2 | อัปเดต **ตารางดัชนี** หน้าแม่ หลังตัด Use Case สถานะ To Do | 🟢 เสร็จ | อัตโนมัติ | หน้าแม่ update: OLS-14 `2→0` · OLS-15 `3→0` · ท้ายตาราง `66→61 Use Case` · โครง/แมโครคงเดิม (diff = 3 จุดตรงเป๊ะ) · อ่านกลับ live ยืนยันแล้ว |
| 3 | รัน verifier เชิงกลซ้ำหน้าที่เพิ่งแก้ (ข้อ 1) ให้ครบ 16/16 | 🟢 เสร็จ | อัตโนมัติ | OLS-13 fixed body ผ่าน 6/6 (โครงคงเดิมเทียบ original) → ครบ 16/16 หน้า |
| 4 | **ด่าน 5 — ตรวจการแสดงผลหน้าจริง (screenshot)** | 🟢 เสร็จ | — | รอบ 2026-07-22: Chrome extension **ต่อ** (Browser 1, local) + login skilllane ค้าง → screenshot ครบ **17 หน้า** (index + 16 ย่อย) ผ่านทุกหน้า: table ไม่แตก · panel warning/note render · status pill + smart-link render · Thai ไม่มีคำพราก · OLS-13 fix "ลงทะเบียนคอร์ส และ เรียนจบคอร์ส" render ถูก · locked term ไม่แตกทุกหน้า (in-app browser ยังเจอ login wall — ใช้ Chrome จริงแทน) |
| 5 | ยืนยัน **ข้อขัดแย้งของแหล่งข้อมูล** ที่ใส่ panel ไว้ | 🟢 เสร็จ (ฝั่งเอกสาร) | BA | เอกสารแสดงสองด้าน + panel กำกับครบแล้ว · **รอ: BA ชี้ขาด** ว่าเอาด้านไหน → ได้คำตอบค่อยแก้หน้า (ระหว่างนั้นช่องที่ผูกอยู่ = TBC ตามคำสั่งผู้ใช้) |
| 6 | ตัดสิน **ข้อความ UC ที่หาต้นทางในตั๋วของตัวเองไม่เจอ** | 🟢 เสร็จ (ฝั่งเอกสาร) | BA | คงข้อความ + panel กำกับแล้ว · **รอ: BA ตัดสิน** เก็บ/ตัด/แก้ข้อความ UC เหล่านั้น |
| 7 | ตัดสิน **ตั๋วที่ดึงข้อมูลไม่สำเร็จ** (timeout เฉพาะใบ) | 🟢 เสร็จ (ฝั่งเอกสาร) | BA | ระบุคีย์ไว้ในหน้าดัชนีแล้ว · **รอ: BA ตัดสิน** ว่าตั๋วพวกนี้ต้องเป็น UC หรือไม่ (ถ้าใช่ → รอบเก็บตกดึงทีละใบ) |
| 8 | ยืนยัน **ความครบถ้วนของ Story** ของ Epic ที่ query ทั้งก้อน timeout | 🟢 เสร็จ (ฝั่งเอกสาร) | BA | panel เตือนบนหน้า Epic นั้นแล้ว · **รอ: BA ยืนยัน** รายชื่อ Story ครบ/ไม่ครบจากฝั่ง Jira |
| 9 | ตัดสินใจว่าจะ **เติมช่อง Status** ใน Page Properties จาก Jira หรือไม่ | 🟢 เสร็จ (ปิด — ถูกกลืนโดยข้อ 12) | ผู้ใช้ | ผู้ใช้ตอบแล้วรอบ 3: ช่องไม่มีข้อมูล/ไม่แน่ใจ → **TBC แดง** ไม่เติม Status จาก Jira · ถ้าภายหลังอยากเติมจริงค่อยเปิดงานใหม่ |
| 10 | ตัดสินใจเรื่อง **สีของ panel** (warning เหลือง vs note เทา) | 🟢 เสร็จ (ปิด — ไม่แก้) | — | ด่าน render รอบ 2 เปิดหน้าจริงครบ 17 หน้า panel แสดงผลถูกทุกหน้า → คง markup เดิม ไม่มีอะไรต้องทำ |
| 11 | **รีวิวเนื้อหา OLS PRD ทั้งชุด** (17 หน้า) — ถูกต้อง/ครบ/ไม่มโน | 🟢 เสร็จ (ตามเงื่อนไขผู้ใช้) | ผู้ใช้ | ผู้ใช้สั่งรอบ 3: จุดหาไม่เจอ/ไม่แน่ใจ → **ใส่ TBC** ไม่ต้องสืบต่อ → งานรีวิวลึก 14 หน้าที่เหลือถูกกลืนเข้าข้อ 12 (จุดไม่แน่ใจที่รู้ตัว = ทุก span "รอข้อมูล" ระบุครบแล้วจากรอบก่อน) · การเทียบต้นทางราย UC (ด่าน 2) ทำครบไปแล้วรอบก่อนหน้า |
| 12 | **แปลง "รอข้อมูล"/จุดไม่แน่ใจ → `TBC` สีแดง** ทั้งชุด 17 หน้า | 🟡 กำลังทำ — 6/17 หน้าเขียนแล้ว | อัตโนมัติ | รอบ 4: connector auth กลับมา (write scope ยืนยันด้วย `getAccessibleAtlassianResources`) → pre-write gate สรุป diff 138 จุด/15 หน้า ผู้ใช้ตอบ **go** แล้ว → เริ่มเขียน target = ทุก `<span data-type="status">รอข้อมูล</span>` → `data-color="red">TBC` (คง local-id เดิม, ไม่แตะ node อื่น) พร้อมอ่านกลับยืนยัน id/เนื้อหาทุกครั้ง (กัน bug connector คืนหน้าผิด) · **เสร็จแล้ว 6 หน้า:** หน้าแม่ (ลบ panel ล้าสมัย "ยังไม่ได้ตรวจ render" — action 4 ปิดไปแล้ว) · OLS-1 (10 จุด) · OLS-2 (13) · OLS-3 (8) · OLS-4 (20) · OLS-5 (5) = TBC 46/138 · **ค้าง 9 หน้า:** OLS-6 (7) · OLS-7 (12) · OLS-8 (4) · OLS-9 (5) · OLS-10 (6) · OLS-11 (9) · OLS-12 (21) · OLS-13 (13) · OLS-17 (5) = เหลือ 92 จุด · OLS-14/15 ไม่มีจุดต้องแก้ (Story ทุกใบยังเป็น To Do ไม่มี description) — ข้ามถาวร ไม่ใช่ค้าง |
| 13 | **เปลี่ยน emoji หน้าชื่อเพจ** 17 หน้า ตามรูปแนบ (ไอคอนลูกศรวนสีน้ำเงิน 🔄) | 🟢 เสร็จ (ฝั่ง AI — ตันเครื่องมือ ส่งมอบให้ทำมือ) | ผู้ใช้ | ตรวจแล้ว: page emoji = content property (`emoji-title-published`) · `updateConfluencePage` **ไม่มี field emoji** และ MCP ไม่มี tool set-content-property → ตั้งผ่าน tool ไม่ได้จริง · **รอ: ผู้ใช้ทำมือใน Confluence UI 17 หน้า** (ยืนยัน emoji ตรงตัวจากรูปแนบก่อนตั้ง — เดาไว้: 🔄) หรือหา token REST API มาให้ค่อยเปิดทางอัตโนมัติ |

**เลนด์:** 🟢 เสร็จ · 🟡 กำลังทำ · 🔴 ค้าง (ทำต่อได้เลย) · 🔵 รอคนตัดสิน · ⛔ ตรวจไม่ได้ · ⚪ ตัวเลือก

---

## บริบทงาน

- **เป้าหมาย:** เติมโครง PRD ราย Epic ของ **subsystem OLS** ให้ครบทุก Epic — 1 หน้าต่อ 1 Epic
  ภายในแต่ละหน้าแจกแจง Use Case ทุกตัวของ Epic นั้น
- **แหล่งข้อมูลเดียว:** Jira project ของ OLS (ไม่ใช้แหล่งอื่น ไม่ลอกจากเอกสาร BA เดิม)
- **กติกาที่ยึดตลอด:**
  - 1 Story = 1 Use Case · Story ที่เป็น improvement นับเป็น UC แยก · Bug ไม่นับเป็น UC
  - ค่าที่ Story ไม่ได้ระบุ → "รอข้อมูล" ห้ามเดา ห้ามเติมให้ดูครบ
  - ข้อความขีดฆ่าใน Jira = requirement ที่ยกเลิกแล้ว → ครอบ `<s>` ไม่ยกมาเป็นกฎที่ใช้อยู่
  - ทำเฉพาะหน้าของเรา **ห้ามแตะหน้าเอกสารของ BA และห้ามแก้ Jira**

## สิ่งที่ทำเสร็จแล้ว

- ✅ สร้างหน้าย่อยราย Epic ครบทุก Epic ในขอบเขต + ตั้งชื่อรูปแบบเดียวกันทั้งชุด
- ✅ เติมตารางดัชนีหน้าแม่ (รอบแรก) พร้อม panel ระบุข้อจำกัดที่พิสูจน์ไม่ได้
- ✅ **ตรวจแหล่งข้อมูลราย UC ทั้งชุด (ด่าน 2)** — เทียบทุก UC กับตั๋ว Jira ต้นทางใหม่ทีละใบ
  - พบเนื้อจากตั๋วผิดใบปนเข้ามา **3 จุด** → แก้แล้วทั้งหมด
  - พบตั๋วขัดกันเองเพิ่ม → ใส่ panel ไม่เลือกข้าง
- ✅ **ตรวจเชิงกล (ด่าน 3–4)** ผ่าน 15/16 หน้า → **ปิดเป็น 16/16 แล้ว** (แก้ OLS-13 คำพราก)
- ✅ ตัด Use Case สถานะ "To Do" ออกจาก 2 หน้า (คงหน้าไว้ + ใส่หมายเหตุระบุคีย์ + สถานะ)
- ✅ เพิ่ม panel กำกับข้อความ UC ที่สืบต้นทางในตั๋วตัวเองไม่ได้
- ✅ **รอบเก็บงานเขียน (2026-07-21):** ปิด action 1 (OLS-13 คำพราก) + action 2 (ดัชนี → รวม 61) เขียนลง Confluence จริง อ่านกลับยืนยันทั้งคู่ · เหลือเฉพาะด่าน 5 (render) ที่ยัง ⛔

## ตัวบล็อกสิทธิ์เขียน connector — ✅ ปลดแล้ว (รอบ 2026-07-21)

Step 0 preflight 2 เงื่อนไข ตอนนี้ผ่านทั้งคู่:

1. **scope `write:page:confluence`** — ✅ (ยืนยันด้วย `getAccessibleAtlassianResources`)
2. **tool เขียน (`updateConfluencePage` / `createConfluencePage`) ใน session** — ✅ เข้า manifest แล้วใน session ใหม่

> บทเรียน (คงไว้): connector อ่านได้ ≠ เขียนได้ · เปิด scope แล้วยังต้อง **เปิด session ใหม่ / reconnect MCP**
> ก่อน tool เขียนจะเข้า manifest — เปิดสิทธิ์กลาง session ไม่พอ · session นี้เปิดใหม่จึงเขียนได้จริง

> ⚠️ **รอบ 3 (2026-07-22): auth หลุดทั้ง connector** — session รอบ 3 ระบบแจ้ง Atlassian MCP
> "ต้อง authenticate ก่อนใช้" และไม่มี tool Confluence/Jira โผล่ใน session เลย (อ่านก็ไม่ได้) ·
> auth เป็นราย session/ราย connection ไม่ใช่ตั้งครั้งเดียวถาวร → ก่อนเริ่มรอบเขียนทุกครั้งต้องเช็ก
> Step 0 ใหม่เสมอ และถ้าหลุดให้ผู้ใช้ re-auth ก่อน (claude.ai connector settings / `/mcp`)

## ทำต่อรอบหน้า (สิ่งที่เหลือ)

**สถานะรวม (รอบ 4):** connector auth กลับมา → เขียนจริงแล้ว **6/17 หน้า** (TBC 46/138 จุด)
เหลือ **9 หน้า / 92 จุด** ก่อนปิดข้อ 12 ได้ครบ — งานถูกขัดจังหวะกลางหน้า OLS-6 (อ่านครบ+เตรียม
substitution แล้ว ยังไม่ได้ยิง `updateConfluencePage`)

ลำดับงานรอบหน้า (ต่อจากนี้ได้เลย ไม่ต้องรอ auth ใหม่ — connector auth คงอยู่ตลอด session ปัจจุบัน,
แต่ auth เป็นราย session ถ้าเปิด session ใหม่ต้องเช็ก Step 0 ใหม่เสมอ):

1. **OLS-6 (7 จุด)** — อ่านครบแล้ว (id `3711795201` ยืนยันตรง) เตรียม body ใหม่ไว้แล้วที่
   scratchpad (`ols6_new_body.html`, TBC 7/7 ตรง) → แค่ยิง `updateConfluencePage` + อ่านกลับยืนยัน
2. **OLS-7 (12) · OLS-8 (4) · OLS-9 (5) · OLS-10 (6) · OLS-11 (9) · OLS-12 (21) · OLS-13 (13) ·
   OLS-17 (5)** — ทำตามลำดับเดิม: fetch html (เช็ก id ตรง) → substitute
   `data-color="neutral">รอข้อมูล</span>` → `data-color="red">TBC</span>` (string เดียว ปลอดภัย
   ไม่ชนกับ "To Do" เพราะ text ต่างกัน) → `updateConfluencePage` → อ่านกลับด้วย `markdown` นับจำนวน
   TBC ให้ตรงเป้า
3. **ปิด Action 12** เมื่อครบ 17/17 (138/138 จุด) → รัน**รีวิว 5 ชั้นซ้ำ** (โครง/ฟอแมตยังคงเดิมแน่นอน
   เพราะแก้แค่ text+color ใน node เดิม แต่ต้อง re-screenshot ยืนยัน render จริงอีกรอบตามกฎ
   "แก้ 1 จุด = รีวิวใหม่ทั้ง 5 ชั้น")
4. **ฝั่งคน (remark รวม — รอใครทำไร, ไม่เปลี่ยนจากรอบ 3):**
   - **BA:** ชี้ขาดข้อ 5–8 (ข้อขัดแย้งแหล่งข้อมูล · UC สืบต้นทางไม่ได้ · ตั๋ว timeout รายใบ · ความครบ
     Story ของ Epic ที่ query timeout) — ระหว่างรอ ช่องที่เกี่ยว = TBC (เขียนแล้วในหน้าที่ทำไปแล้ว)
   - **ผู้ใช้:** ตั้ง emoji หน้าเพจ 17 หน้ามือใน UI (ข้อ 13 — tool ตัน)
   - **AI (รอบหน้า):** เขียน TBC 9 หน้าที่เหลือ + รีวิว 5 ชั้นซ้ำตอนครบ

## ความเสี่ยงที่พบระหว่างงาน (ต้องระวังในรอบต่อไป)

### connector คืนผลผิดแบบสุ่ม — พิสูจน์แล้ว ไม่ใช่การเดา
- `getConfluencePage` ด้วย id เดิม เคยคืน **หน้าคนละหน้า** และครั้งหนึ่งผิดครบ 5/5
- `searchJiraIssuesUsingJql` เคยขอตั๋วใบหนึ่ง แต่ **คืนตั๋วคนละใบ**
- `updateConfluencePage` เคยคืน **response เป็นหน้าอื่น** (แต่เขียนลงถูกหน้า — response เชื่อไม่ได้)
- **อาการผูกกับ `contentFormat`**: `html` คืนถูกราว 1 ใน 3 · `markdown`/`adf` คืนถูกทุกครั้งที่ทดสอบ

**มาตรการที่ใช้ (ต้องคงไว้ทุกครั้งที่แตะ connector):**
1. ทุกการอ่าน → เทียบ id/คีย์ที่คืนกลับกับที่ขอ **ไม่ตรง = ทิ้งทันที ดึงใหม่** (สูงสุด 5–8 ครั้ง)
2. อ่านยืนยันตัวตนด้วย `markdown`/`adf` ก่อน แล้วค่อยใช้ `html` เมื่อจำเป็นต้องได้ body
3. หลัง `update` **ห้ามเชื่อ response** ต้องอ่านกลับด้วย `markdown` ยืนยันว่าเขียนถูกหน้า
4. ครบโควตายังไม่ตรง → บันทึกว่า "ตรวจไม่ได้" **ห้ามเดา**

### Jira query บางแบบ timeout
- `parent = "<EPIC>"` และ `key in (หลายใบ)` — timeout บ่อย โดยเฉพาะ Epic ใหญ่
- `key = <ใบเดียว>` — ตอบเร็ว ใช้ได้จริง → **ดึงทีละใบเสมอ**
- มีบางตั๋วที่ timeout เฉพาะใบ แม้ query เบา (ดู action ข้อ 7)

## นิยาม "เสร็จ"

ส่งมอบได้เมื่อผ่าน **5 ด่าน 5/5**:
1. ตรงตามที่ยืนยัน — ✅
2. ทุกค่ามีที่มา — ✅ (เชิงเนื้อหา) · เหลือ 🔵 รอ BA ชี้ขาดข้อขัดแย้ง (ข้อ 5–8)
3. โครง/ฟอแมตคงเดิม — ✅ (เชิงกล ครบ 16/16 · ปิด action 1+3 แล้ว)
4. ศัพท์/ตัวเลข/คำพราก — ✅ (ปิด action 1 แล้ว · verifier term-check ผ่านทุกหน้า)
5. render บนหน้าจริง — ✅ (action ข้อ 4 ปิดแล้ว 2026-07-22 · screenshot ครบ 17 หน้า ผ่านทุกหน้า)

> ครบ **5/5 เชิงกล + render** แล้วก่อนรอบ 4 (re-verify 2026-07-22 รอบ 2) แต่ **action 12 (เขียน TBC)
> กำลังทำอยู่ ยังไม่ครบ** → ยังปิดเป็น "ส่งมอบได้" ไม่ได้จนกว่าจะครบ 17/17 หน้า **แล้วรีวิว 5 ชั้นซ้ำ**
> (กฎ "แก้ 1 จุด = รีวิวใหม่ทั้ง 5 ชั้น" ยังใช้ แม้เป็นการแก้เชิงกลล้วน ๆ)
> **รอใครทำไร (สรุปล่าสุด รอบ 4):** BA → ชี้ขาดข้อ 5–8 (TBC ไว้ระหว่างรอ) · ผู้ใช้ → ตั้ง emoji มือ
> 17 หน้า (ข้อ 13 — tool ตัน) · AI รอบหน้า → เขียน TBC ต่อจาก OLS-6 จนครบ 9 หน้าที่เหลือ (92 จุด)
> แล้วรีวิว 5 ชั้นซ้ำปิดงาน
