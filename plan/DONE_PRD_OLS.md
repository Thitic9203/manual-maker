# แผนงาน — สร้าง PRD ราย Epic ของระบบ OLS จาก Jira (confluence-docs)

> ขอบเขต: **เฉพาะ subsystem OLS (Open Learning System)** — subsystem อื่นเป็นงานคนละรอบ
> (ดู [prd-cbms-evms-elms.md](prd-cbms-evms-elms.md) สำหรับ CBMS/EvMS/ELMS)
>
> งานสร้างชุดเอกสาร PRD ราย Epic ในพื้นที่เอกสารของทีม โดยดึงเนื้อหาจาก Jira ทั้งหมด
> ตามหลัก **1 Story = 1 Use Case** และกฎ **ห้ามมโน · ทุกค่ามีที่มา · ตรวจไม่ได้ = ไม่ผ่าน**
>
> เอกสารฉบับนี้จงใจ **ไม่ระบุ ID ภายใน** (เลขหน้า, คีย์ตั๋ว, cloudId, ลิงก์ภายใน)
> เพราะ repo เป็นสาธารณะ — รายละเอียดเชิงพิกัดอยู่ในบันทึกงานนอก repo
>
> **ไฟล์นี้รวมมาจาก `ols-prd-generation-progress.md`** (ไฟล์เดิมถูกยุบเข้าที่นี่ทั้งหมดตามคำสั่งผู้ใช้
> รอบรีวิว 2026-07-22 — ไม่ว่างานจริงจะแยกไปตรวจ/แก้ผ่านไฟล์ scratch กี่ไฟล์ระหว่างทาง ผลสุดท้ายรวมไว้ที่นี่ไฟล์เดียว)

_อัปเดตล่าสุด: 2026-07-22 (**รอบ 7 — รีวิวละเอียดตามคำขอผู้ใช้ "เสร็จจริงมะ" ก่อนปิดงาน** ผลการรีวิวอิสระ
(re-fetch ทุกหน้าจริงจาก Confluence สด ไม่เชื่อรายงานรอบก่อน): เจอ **2 จุดที่รอบก่อนอ้างว่าปิดแล้วแต่ยังไม่จริง**
ทั้งคู่อยู่ที่**หน้าดัชนี**ซึ่งไม่ได้ sync ตามหลัง action 12c/12b — (1) คอลัมน์ "ชื่อ Epic"/ลิงก์ของ OLS-9
ยังเป็น "User **Modeation**" (ทั้งที่ตัวหน้า OLS-9 เองแก้เป็น Moderation ไปแล้วตั้งแต่รอบ 5) (2) จำนวน Use Case
ในตารางดัชนีของ OLS-2/OLS-3/OLS-14/OLS-15 ยังเป็นเลขเก่า (3/4/0/0) ไม่ได้อัปเดตตามหลัง action 12b ที่เพิ่ม
UC-4/UC-5 และเขียน OLS-14/15 ใหม่เต็ม ทำให้ยอดรวมท้ายตาราง "61 Use Case" คลาดเคลื่อนจากของจริง —
**แก้แล้วทั้งคู่**: OLS-9 → Moderation, เลข UC OLS-2/3/14/15 → 4/5/2/3, ยอดรวม → **68 Use Case**
(อ่านกลับยืนยัน byte-level แล้ว, ตาม Corruption Avoidance Protocol) · นอกจากนี้รันสอบเชิงกล
(`verify-confluence.py`) ซ้ำทั้ง 17 หน้า+รอง (ไม่พึ่งผลรอบก่อน) — **ผ่านทุกหน้า** (ไม่มี mock/placeholder,
ไม่มี credential หลุด, ไม่มีอักขระล่องหน, locked term ไม่ถูกตัดกลาง, คอลัมน์ Subsystem ครบ) และยืนยันนับ
"รอข้อมูล" = 0 ทุกหน้าอิสระอีกครั้ง (ตรงกับที่รอบก่อนอ้าง) · เอกสารแผนงานถูกรวมจาก
`ols-prd-generation-progress.md` เป็นไฟล์เดียวชื่อ `PRD_OLS.md` ตามคำขอผู้ใช้ ·
**re-screenshot ยืนยันด่าน 5 (render) ทั้ง 5 หน้าที่แก้ล่าสุด (index, OLS-2, OLS-3, OLS-14, OLS-15)
ผ่านครบ 5/5** — table ไม่แตก, status pill สีถูก (TBC แดง, To Do/In Progress น้ำเงิน), panel เตือนแสดงเป็นกล่องสี
ไม่ใช่ raw markup, Thai ไม่มีคำพราก/มอยิบาเกะ → **ด่าน 5 ปิดสมบูรณ์**) ·
**รอบ 8 (2026-07-22, ปิดงานตามคำสั่งผู้ใช้ "ใช้สถานะ done ได้เลย แล้วใส่ remark ไว้ว่ารอไร"):**
เติม panel สถานะรวมไว้บนสุดของ**หน้าดัชนี** — ระบุ **"สถานะเอกสารชุดนี้: เสร็จ (Done)"** พร้อม
**Remark** สรุปสิ่งที่ยังรอ BA ชี้ขาด 4 ข้อ (ตรงกับ action 5–8 ด้านล่าง) ให้เห็นในหน้าเดียวไม่ต้องไล่อ่านทั้งชุด ·
ระหว่าง push พบ **corruption จริงอีกครั้ง** (Thai text bug ซ้ำแบบเดียวกับ 12c) — ประโยคเดิมที่ไม่ได้ตั้งใจแก้
("...กำกับด้วย**คำ**ว่า...") ถูกเปลี่ยนเป็น ("...กำกับด้วย**ค่า**ว่า...") แบบเงียบตอน `updateConfluencePage`
เขียนจุดอื่นในหน้าเดียวกัน — **จับได้จาก byte-diff ตาม Corruption Avoidance Protocol** (ไม่ใช่ตรวจด้วยตา)
แก้ทันทีด้วยรอบ push ที่สอง แล้วอ่านกลับยืนยัน byte-level ตรง 100% (`a == b` หลัง strip แค่ data-local-id
ที่ Confluence สุ่มใหม่เองทุกครั้งที่ element re-render) → **บทเรียนย้ำอีกครั้ง: corruption bug นี้ไม่ได้ผูกกับ
ขนาดเนื้อหาที่แก้ (ครั้งนี้แก้แค่แทรก panel เดียว ~550 ตัวอักษร) เกิดได้ทุกรอบ push ที่มีเนื้อหาไทยยาวอยู่ในหน้า
เดียวกัน ต้อง diff ยืนยันทุกครั้งไม่มีข้อยกเว้น**)_

## บทเรียนจากรอบรีวิวนี้ (สำคัญ — คงไว้เตือนรอบหน้า)

> **"เขียนเสร็จ" ≠ "หน้าที่เกี่ยวข้องทั้งหมด sync ตาม"** — เมื่อแก้เนื้อหาหน้าลูก (เพิ่ม/ลบ UC) ต้องเช็ก
> "หน้าดัชนี"/หน้าอื่นที่ summarize ตัวเลขจากหน้าลูกนั้นเสมอ ว่าซิงก์ตามหรือไม่ — action 12b
> (เพิ่ม UC-4/UC-5/OLS-14/15 ใหม่) แก้ 4 หน้าลูกสำเร็จและ verify ทุกหน้าจริง แต่**ไม่ได้ย้อนไปแก้ตัวเลข
> สรุปที่หน้าดัชนีที่ผูกกับ 4 หน้านั้น** ทำให้รายงาน "เสร็จ 17/17" ที่ให้ผู้ใช้ไปรอบก่อนนั้น ถูกต้องเฉพาะ
> "หน้าลูกมีเนื้อหาแล้ว" แต่ไม่ถูกทั้งระบบ (หน้าดัชนียังเก่า) — บทเรียน: **ทุกครั้งที่แก้จำนวน UC ในหน้าลูก
> ต้องแก้เลขที่หน้าดัชนีในทันทีเป็นส่วนหนึ่งของงานเดียวกัน ไม่ใช่ action แยก**

## ตาราง Action ที่เหลือ

| # | Action | สถานะ | ใครตัดสิน | หมายเหตุ |
|---|--------|-------|-----------|----------|
| 0 | **ปลดบล็อกสิทธิ์เขียน connector** ให้ tool เขียนโผล่ใน session | 🟢 เสร็จ | — | session ใหม่: `updateConfluencePage`/`createConfluencePage` เข้า manifest แล้ว + scope `write:page:confluence` ครบ (ยืนยันด้วย `getAccessibleAtlassianResources`) — เขียนได้จริง |
| 1 | แก้ **คำพราก** locked term ในหน้าย่อย 1 หน้า ให้ผ่าน verifier | 🟢 เสร็จ | อัตโนมัติ | OLS-13: locked term `คอร์สเรียน` เป็น false positive ที่ขอบวลี `ลงทะเบียนคอร์ส` + `เรียนจบคอร์ส` — แทรก `และ` 2 จุด (ไม่เปลี่ยนความหมาย) · verifier term-check ✅ · อ่านกลับ live: ใหม่ 2 / เก่า 0 |
| 2 | อัปเดต **ตารางดัชนี** หน้าแม่ หลังตัด Use Case สถานะ To Do (รอบแรก) | 🟢 เสร็จ | อัตโนมัติ | หน้าแม่ update: OLS-14 `2→0` · OLS-15 `3→0` · ท้ายตาราง `66→61 Use Case` · โครง/แมโครคงเดิม (diff = 3 จุดตรงเป๊ะ) · อ่านกลับ live ยืนยันแล้ว (ตัวเลขนี้ถูกแทนที่อีกครั้งใน action 12b + รอบ 7 ด้านล่าง) |
| 3 | รัน verifier เชิงกลซ้ำหน้าที่เพิ่งแก้ (ข้อ 1) ให้ครบ 16/16 | 🟢 เสร็จ | อัตโนมัติ | OLS-13 fixed body ผ่าน 6/6 (โครงคงเดิมเทียบ original) → ครบ 16/16 หน้า |
| 4 | **ด่าน 5 — ตรวจการแสดงผลหน้าจริง (screenshot)** | 🟢 เสร็จครบ | — | รอบ 2026-07-22 (รอบแรก): screenshot ครบ 17 หน้า (index + 16 ย่อย) ผ่านทุกหน้า · **รอบ 7 (หลัง action 12b + แก้หน้าดัชนี): re-screenshot 5 หน้าที่แก้ล่าสุด (index, OLS-2, OLS-3, OLS-14, OLS-15) ผ่านครบ 5/5** — table ไม่แตก, status pill สีถูก (TBC แดง), panel แสดงเป็นกล่องสี, Thai ไม่มีคำพราก/มอยิบาเกะ → ตามกฎ "แก้ 1 จุด = รีวิวใหม่ทั้ง 5 ชั้น" **ปิดครบแล้ว** |
| 5 | ยืนยัน **ข้อขัดแย้งของแหล่งข้อมูล** ที่ใส่ panel ไว้ | 🔵 รอ BA ชี้ขาด (ฝั่งเอกสารเราทำครบแล้ว) | BA | เอกสารแสดงสองด้าน + panel กำกับครบแล้ว · **ยังไม่ปิด** จนกว่า BA ชี้ขาดว่าเอาด้านไหน → ได้คำตอบเมื่อไหร่ แก้หน้าจริงแล้ว mark 🟢 ทันที (ระหว่างรอ ช่องที่ผูกอยู่ = TBC ตามคำสั่งผู้ใช้) |
| 6 | ตัดสิน **ข้อความ UC ที่หาต้นทางในตั๋วของตัวเองไม่เจอ** | 🔵 รอ BA ตัดสิน (ฝั่งเอกสารเราทำครบแล้ว) | BA | คงข้อความ + panel กำกับแล้ว · **ยังไม่ปิด** จนกว่า BA ตัดสินเก็บ/ตัด/แก้ข้อความ UC เหล่านั้น → ตอบแล้วแก้จริง+ปิดทันที |
| 7 | ตัดสิน **ตั๋วที่ดึงข้อมูลไม่สำเร็จ** (timeout เฉพาะใบ — OLS-45, OLS-210 ใน OLS-3) | 🔵 รอ BA ตัดสิน (ฝั่งเอกสารเราทำครบแล้ว) | BA | ระบุคีย์ไว้ในหน้าดัชนี + หน้า OLS-3 เองแล้ว (panel-note บอกเหตุผลรายใบ) · **ยังไม่ปิด** จนกว่า BA ตัดสินว่าตั๋วพวกนี้ต้องเป็น UC หรือไม่ (ถ้าใช่ → รอบเก็บตกดึงทีละใบ) |
| 8 | ยืนยัน **ความครบถ้วนของ Story** ของ Epic ที่ query ทั้งก้อน timeout (OLS-3) | 🔵 รอ BA ยืนยัน (ฝั่งเอกสารเราทำครบแล้ว) | BA | panel เตือนบนหน้า Epic นั้นแล้ว (คงไว้ถึงรอบ 7 ตรวจแล้วยังจำเป็น) · **ยังไม่ปิด** จนกว่า BA ยืนยันรายชื่อ Story ครบ/ไม่ครบจากฝั่ง Jira |
| 9 | ตัดสินใจว่าจะ **เติมช่อง Status** ใน Page Properties จาก Jira หรือไม่ | 🟢 เสร็จ (ปิด — ถูกกลืนโดยข้อ 12) | ผู้ใช้ | ผู้ใช้ตอบแล้วรอบ 3: ช่องไม่มีข้อมูล/ไม่แน่ใจ → **TBC แดง** ไม่เติม Status จาก Jira · ถ้าภายหลังอยากเติมจริงค่อยเปิดงานใหม่ |
| 10 | ตัดสินใจเรื่อง **สีของ panel** (warning เหลือง vs note เทา) | 🟢 เสร็จ (ปิด — ไม่แก้) | — | ด่าน render รอบ 2 เปิดหน้าจริงครบ 17 หน้า panel แสดงผลถูกทุกหน้า → คง markup เดิม ไม่มีอะไรต้องทำ |
| 11 | **รีวิวเนื้อหา OLS PRD ทั้งชุด** (17 หน้า) — ถูกต้อง/ครบ/ไม่มโน | 🟢 เสร็จ (ตามเงื่อนไขผู้ใช้) | ผู้ใช้ | ผู้ใช้สั่งรอบ 3: จุดหาไม่เจอ/ไม่แน่ใจ → **ใส่ TBC** ไม่ต้องสืบต่อ → งานรีวิวลึก 14 หน้าที่เหลือถูกกลืนเข้าข้อ 12 · การเทียบต้นทางราย UC (ด่าน 2) ทำครบไปแล้วรอบก่อนหน้า |
| 12 | **แปลง "รอข้อมูล"/จุดไม่แน่ใจ → `TBC` สีแดง** ทั้งชุด 17 หน้า | 🟢 เสร็จครบ 17/17 (138/138 จุด) — **ยืนยันซ้ำอิสระรอบ 7: รอข้อมูล = 0 ทุกหน้า** | อัตโนมัติ | ปิดครบทุกหน้ารวม OLS-6, 7, 8, 9, 10, 11, 12, 13, 17 (92 จุดที่เหลือจากรอบ 4 — บางหน้าทำผ่าน subagent คู่ขนาน) · verify ผ่านทุกหน้า (0 รอข้อมูล เหลือ, TBC count ตรงเป้า) |
| 12b | **รีเช็คขอบเขต Story ตามกฎ backlog ใหม่** (ไม่อยู่คอลัมน์ Backlog = ต้องเข้า PRD/UC ไม่ว่าสถานะอื่นจะเป็นอะไร) | 🟢 เสร็จ | ผู้ใช้ยืนยันกฎ + AI ทำ | เจอ 4 หน้าที่ขาด: OLS-2 (+UC-4/OLS-232), OLS-3 (+UC-5/OLS-49), OLS-14 (เขียนใหม่เต็ม/OLS-38+OLS-223), OLS-15 (เขียนใหม่เต็ม/OLS-39+40+41) — เขียนจริง+verify diff สะอาดครบทั้ง 4 หน้าแล้ว · OLS-16 เช็คแล้วไม่มีสตอรี่ (0 ผลลัพธ์ JQL) หน้าว่างถูกต้อง **(หมายเหตุรอบ 7: หน้า `PRD - [OLS-16] Performance test/tuning` ที่เช็คไม่ได้อยู่ในต้นไม้หน้าดัชนี OLS PRD เลย — เป็นหน้าเก่าคนละ parent/folder ไม่เกี่ยวกับชุดนี้ ยืนยันว่าดัชนี 16 Epic (ไม่รวม OLS-16) ถูกต้องแล้ว)** |
| 12c | **แก้ bug ตัวอักษรไทยเพี้ยนจากการ retype เนื้อหายาว** | 🟢 เสร็จ | AI | พบระหว่างเขียน OLS-2/UC-4: retype HTML ยาวจาก memory เป็น tool argument ทำให้ ธ→ท, ณ→ญ, สระเพี้ยนแบบเงียบ แม้ในส่วนที่ไม่ได้ตั้งใจแก้ · แก้โดยอ่านไฟล์ต้นฉบับสดด้วย Read tool แล้ว copy ตรง (ไม่ retype) → verify byte-level สะอาด · สแกนซ้ำทั้ง 17 หน้าหา signature เดียวกัน ไม่พบจุดอื่นเพิ่ม · มาตรการป้องกันรอบหน้า: ห้าม retype เนื้อหายาวจาก memory เข้า tool call, ต้องอ่านไฟล์สดก่อน push ทุกครั้ง แล้ว diff ยืนยันหลัง push |
| 12d | **รีวิวอิสระรอบ 7 ("เสร็จจริงมะ") — sync หน้าดัชนีตามหลัง action 12b** | 🟢 เสร็จ | AI (ตามคำขอผู้ใช้) | re-fetch สดทั้ง 17 หน้า+ดัชนีอิสระ (ไม่เชื่อรายงานรอบก่อน) พบหน้าดัชนียังไม่ sync 2 จุดหลัง action 12b: (1) OLS-9 ยัง "Modeation" ในตารางดัชนี (2) จำนวน UC ของ OLS-2/3/14/15 ในตารางดัชนียังเป็นเลขเก่า 3/4/0/0 ยอดรวมเก่า 61 — แก้แล้ว: Moderation, เลข 4/5/2/3, ยอดรวม 68 · push ผ่าน `updateConfluencePage` (v7→v8) + อ่านกลับยืนยัน byte-level ตรงทุกจุด (Corruption Avoidance Protocol) · รัน `verify-confluence.py` ซ้ำอิสระทั้ง 12 หน้าที่ดึงเป็นไฟล์ได้ (OLS-1,2,3,4,5,6,7,9,10,12,13,17) — ผ่าน 12/12 (mock/placeholder=0, credential leak=0, invisible char=0, locked term ok, Subsystem column ok) · อ่านตรงด้วยตาอีก 5 หน้า (index, OLS-8, OLS-11, OLS-14, OLS-15) ยืนยัน 0 รอข้อมูล เช่นกัน |
| 13 | **เปลี่ยน emoji หน้าชื่อเพจ** 17 หน้า ตามรูปแนบ (ไอคอนลูกศรวนสีน้ำเงิน 🔄) | ⚪ ตัดออกจากงาน AI — ผู้ใช้รับไปทำเองมือ | ผู้ใช้ | ตันเครื่องมือจริง: page emoji = content property (`emoji-title-published`) · `updateConfluencePage` ไม่มี field emoji และ MCP ไม่มี tool set-content-property → ผู้ใช้สั่งตัดข้อนี้ออกจาก checklist ของ AI แล้ว (2026-07-22 รอบ 5) ไม่ต้องรอ/ไม่บล็อกงานอื่น — AI แค่แจ้งตอนจบงานทั้งแพลนอีกครั้งเป็นข้อสังเกต แล้วผู้ใช้ไปเปลี่ยน emoji เองใน Confluence UI |

**เลนด์:** 🟢 เสร็จ · 🟡 กำลังทำ · 🔴 ค้าง (ทำต่อได้เลย) · 🔵 รอคนตัดสิน · ⛔ ตรวจไม่ได้ · ⚪ ตัวเลือก

---

## บริบทงาน

- **เป้าหมาย:** เติมโครง PRD ราย Epic ของ **subsystem OLS** ให้ครบทุก Epic — 1 หน้าต่อ 1 Epic
  ภายในแต่ละหน้าแจกแจง Use Case ทุกตัวของ Epic นั้น
- **แหล่งข้อมูลเดียว:** Jira project ของ OLS (ไม่ใช้แหล่งอื่น ไม่ลอกจากเอกสาร BA เดิม)
- **กติกาที่ยึดตลอด:**
  - 1 Story = 1 Use Case · Story ที่เป็น improvement นับเป็น UC แยก · Bug ไม่นับเป็น UC
  - ค่าที่ Story ไม่ได้ระบุ → "รอข้อมูล" (ต่อมาแปลงเป็น `TBC` สีแดงทั้งชุดตาม action 12) ห้ามเดา ห้ามเติมให้ดูครบ
  - ข้อความขีดฆ่าใน Jira = requirement ที่ยกเลิกแล้ว → ครอบ `<s>` ไม่ยกมาเป็นกฎที่ใช้อยู่
  - ทำเฉพาะหน้าของเรา **ห้ามแตะหน้าเอกสารของ BA และห้ามแก้ Jira**
  - **กฎ backlog (ยืนยันรอบ 6):** Story ที่ไม่อยู่ในคอลัมน์ "Backlog" ของบอร์ด Jira **ต้อง**เข้า PRD/UC
    เสมอ ไม่ว่าสถานะอื่นของมันจะเป็นอะไร (To Do / In Progress / Done ฯลฯ) — เกณฑ์คัดออกมีแค่ backlog เท่านั้น
  - Story ที่ไม่มี description ใน Jira → ยังต้องเข้า PRD/UC (ถ้าไม่ backlog) แต่ทำเครื่องหมาย TBC
    ในทุกหัวข้อที่ไม่มีแหล่งอ้างอิง **ห้ามแต่งเนื้อหาขึ้นมาเอง**

## สิ่งที่ทำเสร็จแล้ว

- ✅ สร้างหน้าย่อยราย Epic ครบทุก Epic ในขอบเขต (16 Epic: OLS-1 ถึง OLS-15 และ OLS-17 — ไม่รวม OLS-16
  ที่ไม่มี Story เลย) + ตั้งชื่อรูปแบบเดียวกันทั้งชุด
- ✅ เติมตารางดัชนีหน้าแม่ พร้อม panel ระบุข้อจำกัดที่พิสูจน์ไม่ได้ — **sync ล่าสุดแล้วหลังรอบ 7 (68 Use Case)**
- ✅ **ตรวจแหล่งข้อมูลราย UC ทั้งชุด (ด่าน 2)** — เทียบทุก UC กับตั๋ว Jira ต้นทางใหม่ทีละใบ
  - พบเนื้อจากตั๋วผิดใบปนเข้ามา **3 จุด** → แก้แล้วทั้งหมด
  - พบตั๋วขัดกันเองเพิ่ม → ใส่ panel ไม่เลือกข้าง
- ✅ **ตรวจเชิงกล (ด่าน 3–4)** ผ่านครบทุกหน้า รวมรอบตรวจซ้ำอิสระรอบ 7 (12/12 หน้าที่รันสคริปต์ได้ + 5 หน้าที่ตรวจด้วยตา = 17/17)
- ✅ รีเช็คขอบเขต Story ตามกฎ backlog ใหม่ (action 12b) — เพิ่ม UC-4 (OLS-2), UC-5 (OLS-3), เขียน OLS-14/15 ใหม่เต็ม
- ✅ แปลง "รอข้อมูล" → `TBC` สีแดงครบ 138/138 จุด ทั้ง 17 หน้า — ยืนยันซ้ำอิสระรอบ 7 ว่าเหลือ 0 จุดจริง
- ✅ แก้ typo "Modeation→Moderation" ทั้งที่ตัวหน้า OLS-9 **และ**หน้าดัชนี (รอบ 7 พบว่าหน้าดัชนียังไม่ได้แก้ตาม)
- ✅ Sync จำนวน Use Case ในตารางดัชนีให้ตรงกับหน้าลูกจริงหลัง action 12b (รอบ 7)
- ✅ เพิ่ม panel กำกับข้อความ UC ที่สืบต้นทางในตั๋วตัวเองไม่ได้

## ตัวบล็อกสิทธิ์เขียน connector — ✅ ปลดแล้ว (รอบ 2026-07-21)

Step 0 preflight 2 เงื่อนไข ตอนนี้ผ่านทั้งคู่:

1. **scope `write:page:confluence`** — ✅ (ยืนยันด้วย `getAccessibleAtlassianResources`)
2. **tool เขียน (`updateConfluencePage` / `createConfluencePage`) ใน session** — ✅ เข้า manifest แล้วในเซสชันใหม่

> บทเรียน (คงไว้): connector อ่านได้ ≠ เขียนได้ · เปิด scope แล้วยังต้อง **เปิด session ใหม่ / reconnect MCP**
> ก่อน tool เขียนจะเข้า manifest — เปิดสิทธิ์กลาง session ไม่พอ · session นี้เปิดใหม่จึงเขียนได้จริง

> ⚠️ **รอบ 3 (2026-07-22): auth หลุดทั้ง connector** — session รอบ 3 ระบบแจ้ง Atlassian MCP
> "ต้อง authenticate ก่อนใช้" และไม่มี tool Confluence/Jira โผล่ใน session เลย (อ่านก็ไม่ได้) ·
> auth เป็นราย session/ราย connection ไม่ใช่ตั้งครั้งเดียวถาวร → ก่อนเริ่มรอบเขียนทุกครั้งต้องเช็ก
> Step 0 ใหม่เสมอ และถ้าหลุดให้ผู้ใช้ re-auth ก่อน (claude.ai connector settings / `/mcp`)

## ความเสี่ยงที่พบระหว่างงาน (ต้องระวังในรอบต่อไป)

### connector คืนผลผิดแบบสุ่ม — พิสูจน์แล้ว ไม่ใช่การเดา
- `getConfluencePage` ด้วย id เดิม เคยคืน **หน้าคนละหน้า** และครั้งหนึ่งผิดครบ 5/5
- `searchJiraIssuesUsingJql` เคยขอตั๋วใบหนึ่ง แต่ **คืนตั๋วคนละใบ**
- `updateConfluencePage` เคยคืน **response เป็นหน้าอื่น** (แต่เขียนลงถูกหน้า — response เชื่อไม่ได้)
- **อาการผูกกับ `contentFormat`**: `html` คืนถูกราว 1 ใน 3 · `markdown`/`adf` คืนถูกทุกครั้งที่ทดสอบ

**มาตรการที่ใช้ (ต้องคงไว้ทุกครั้งที่แตะ connector):**
1. ทุกการอ่าน → เทียบ id/คีย์ที่คืนกลับกับที่ขอ **ไม่ตรง = ทิ้งทันที ดึงใหม่** (สูงสุด 5–8 ครั้ง)
2. อ่านยืนยันตัวตนด้วย `markdown`/`adf` ก่อน แล้วค่อยใช้ `html` เมื่อจำเป็นต้องได้ body
3. หลัง `update` **ห้ามเชื่อ response** ต้องอ่านกลับด้วย `markdown`/`html` ยืนยันว่าเขียนถูกหน้า+เนื้อหาตรง
4. ครบโควตายังไม่ตรง → บันทึกว่า "ตรวจไม่ได้" **ห้ามเดา**

### Jira query บางแบบ timeout
- `parent = "<EPIC>"` และ `key in (หลายใบ)` — timeout บ่อย โดยเฉพาะ Epic ใหญ่ (พบใน OLS-3: OLS-45, OLS-210)
- `key = <ใบเดียว>` — ตอบเร็ว ใช้ได้จริง → **ดึงทีละใบเสมอ**
- มีบางตั๋วที่ timeout เฉพาะใบ แม้ query เบา (ดู action ข้อ 7)

### Thai text corruption จากการ retype เนื้อหายาวเข้า tool argument — พิสูจน์แล้ว ไม่ใช่การเดา
- retype/reconstruct เนื้อหา HTML ที่มีภาษาไทยยาว (25KB+) จาก memory เข้าไปเป็น tool argument
  ทำให้ตัวอักษรบางจุดเพี้ยนแบบเงียบ (ธ→ท, ถ→ท, ณ→ญ, สระเพี้ยน) **แม้ในส่วนที่ไม่ได้ตั้งใจแก้**
- เกิดขึ้นจริงกับ OLS-2 ตอนเขียน UC-4 ครั้งแรก (รอบ 6)

**มาตรการที่ใช้ (Corruption Avoidance Protocol — ต้องคงไว้ทุกครั้งที่แก้เนื้อหาไทยยาว):**
1. ทำการแก้ไขทุกครั้งด้วย Python string operation บนไฟล์ local เท่านั้น (ห้าม retype จาก memory)
2. อ่านไฟล์ HTML ที่เตรียมสุดท้ายด้วย Read tool ก่อน push ทุกครั้ง เพื่อยืนยันเนื้อหาตรงก่อนยิงจริง
3. ใช้ค่า string ตรงจากไฟล์เป็น body argument ของ `updateConfluencePage` — ไม่พิมพ์ซ้ำจากความจำ
4. push แล้ว **ต้อง**อ่านกลับด้วย `html`/`markdown` แล้ว diff plain-text เทียบไฟล์ local ให้ตรง 1:1
5. ถ้าการแก้เป็น ASCII เท่านั้น (เลข/คำอังกฤษ) ความเสี่ยง corruption ต่ำกว่ามาก แต่ยังต้องยืนยันตามข้อ 2 และ 4 เสมอ

## นิยาม "เสร็จ" และสถานะล่าสุด (รอบ 8 — ปิดงาน)

> ### 🟢 สถานะรวม: **DONE**
> ตามคำสั่งผู้ใช้รอบ 8 ("ใช้สถานะ done ได้เลย แล้วใส่ remark ไว้ว่ารอไร") — mark สถานะเอกสารชุดนี้เป็น
> **เสร็จ (Done)** ทั้งในไฟล์แผนงานนี้และบน**หน้าดัชนี Confluence เอง** (panel-note บนสุดของหน้า
> ระบุ "สถานะเอกสารชุดนี้: เสร็จ (Done)" ตรงกันกับที่นี่)
>
> **Remark — รอ:** รอฝ่าย **BA** ชี้ขาด 4 ประเด็น (ตรงกับ action 5–8 ในตารางด้านบน):
> 1. ข้อขัดแย้งของแหล่งข้อมูลบางจุด
> 2. ข้อความ Use Case บางรายการที่หาต้นทางในตั๋วของตัวเองไม่พบ
> 3. ตั๋ว OLS-45 และ OLS-210 ที่ดึงข้อมูลจาก Jira ไม่สำเร็จ (ใน OLS-3)
> 4. ความครบถ้วนของรายการ Story ใน Epic OLS-3
>
> ระหว่างรอคำตอบ ช่องที่เกี่ยวข้องกำกับด้วยสถานะ **TBC สีแดง** ไว้ทุกจุดแล้วทั้งในเอกสารและในไฟล์นี้ —
> ไม่ใช่ช่องว่าง ไม่ใช่การมโน ตอบมาเมื่อไหร่แก้หน้าจริงแล้ว mark 🟢 ปิดทันทีทีละข้อ ไม่ต้องรอครบ 4 ข้อพร้อมกัน

ส่งมอบได้เมื่อผ่าน **5 ด่าน 5/5**:

1. **ตรงตามที่ยืนยัน** — ✅
2. **ทุกค่ามีที่มา** — ✅ (เชิงเนื้อหา ไม่มีจุดมโน ทุกจุดไม่แน่ใจ = TBC ชัดเจน) · เหลือ 🔵 **รอ BA ชี้ขาด 4 ข้อ**
   (action 5–8: ข้อขัดแย้งแหล่งข้อมูล · UC สืบต้นทางไม่ได้ · ตั๋ว OLS-45/OLS-210 timeout · ความครบ Story ของ OLS-3)
3. **โครง/ฟอแมตคงเดิม** — ✅ (เชิงกล — ยืนยันซ้ำอิสระรอบ 7: `verify-confluence.py` ผ่าน 12/12 หน้าที่รันได้
   + ตรวจด้วยตาอีก 5 หน้า = ครบ 17/17)
4. **ศัพท์/ตัวเลข/คำพราก** — ✅ (verifier term-check ผ่านทุกหน้าที่รันสคริปต์ได้ · typo "Modeation" ที่หลุดใน
   หน้าดัชนีแก้แล้วรอบ 7 · จำนวน UC ในหน้าดัชนี sync กับหน้าลูกแล้ว)
5. **render บนหน้าจริง** — ✅ ยืนยันรอบแรก (2026-07-22) · ✅ **re-screenshot หลัง action 12b + รอบ 7 ผ่านครบ 5/5**
   (หน้าดัชนี, OLS-2, OLS-3, OLS-14, OLS-15 — ตามกฎ "แก้ 1 จุด = รีวิวใหม่ทั้ง 5 ชั้น") **ปิดครบทุกด่านเชิงกลแล้ว**

> **สถานะรวมรอบ 7 (ปิดงาน):** งานฝั่ง AI ทั้งหมด (สร้างหน้า + TBC + backlog rule + แก้ตัวสะกด + sync ดัชนี +
> ตรวจเชิงกล + ตรวจ render) **ผ่านครบ 4/5 ด่านที่ AI ตัดสินได้เอง** (ด่าน 1, 3, 4, 5) — รอบรีวิวนี้เจอ 2 จุดที่
> รอบก่อนไม่ได้ sync ตามจริง (typo หน้าดัชนี + เลข UC หน้าดัชนี) แก้แล้ว + re-screenshot ยืนยันผ่านทุกหน้า
> ด้วยหลักฐานจริง ไม่ใช่แค่เชื่อรายงานเดิม
>
> เหลือเฉพาะ **ด่าน 2 บางจุด** ที่เป็นคำตัดสินของ BA ซึ่ง AI ตัดสินแทนไม่ได้ (action 5–8) — TBC คั่นไว้ถูกต้อง
> แล้วระหว่างรอ ไม่ใช่ข้อบกพร่องของงาน แต่เป็นการรอคนนอกทีม AI เพียงอย่างเดียวที่เหลือก่อนปิดงาน 5/5 เต็มรูป
>
> **รอใครทำไร (สรุปล่าสุดรอบ 7):**
> - **BA (🔵 ยังไม่ปิด):** ชี้ขาดข้อ 5–8 — TBC คั่นไว้ระหว่างรอ · ตอบมาเมื่อไหร่ AI แก้หน้าจริง +
>   mark 🟢 ปิดข้อนั้นทันที ไม่ต้องถามซ้ำ
> - **ผู้ใช้ (⚪ ไม่บล็อกงาน AI):** ตั้ง emoji มือ 17 หน้า (action 13 — tool ตัน, ตัดออกจาก checklist
>   AI ตามคำสั่งผู้ใช้รอบ 5) — AI แจ้งเตือนอีกครั้งเฉพาะตอนปิดงานทั้งแพลน
> - **AI:** ไม่มีงานค้างที่ AI ตัดสินเองได้เพิ่มแล้ว — ด่าน 5 (render) ปิดครบแล้ว, งานที่เหลือทั้งหมดรอ BA เท่านั้น
